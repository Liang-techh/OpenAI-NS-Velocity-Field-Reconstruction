# Hierarchy-bound conditional Picard tail enclosure provenance

## Scope

This increment is stacked on Agent 2 PR #348 and remains inside Issue #2 / Stage 2. It joins the already hierarchy-owned finite Picard derivative triangle

`W0: eta^3 -> W1: eta^2 -> W2: eta -> W3: value`

into the exact retained value prefix `W0 + W1 + W2 + W3`, then binds that prefix to the existing Lemma 5.1 / Eq. (5.8) tail arithmetic at the first omitted term `k=4`.

The retained prefix accepts only `Section5LowerHistorySixthMixedHierarchy`. Each term is regenerated by the landed hierarchy-owned forcing/matrix/Picard bridges. No caller `SourceJet`, repair jet, forcing derivative table, matrix derivative table, independently sampled `V/X`, finite difference, fitted residual, or generic cutoff is introduced.

## Paper / implementation anchors

- Lemma 5.1 and Eq. (5.7): `W_n = sum_{k>=0} K^k G f_n`.
- Eq. (5.8): analytic majorant for the `k`-th Picard term on a smaller complex eta strip.
- Existing theorem-side arithmetic: `src/openai_ns_reconstruction/background_picard_bounds.py`.
- Existing hierarchy-owned terms: PRs #326, #341, #344 and #348, with the forcing/matrix ownership stack beneath them (#319, #322, #332, #336).

## New contract

`HierarchyBoundPicardMajorantInputs` binds the external Eq. (5.8) inputs `C_n`, radial extent, `rho`, and `rho_prime` to one exact runtime hierarchy instance and recursive coefficient order. It recomputes the existing Eq. (5.8) two-parity tail certificate at `start_order=4` and fails closed if that tail is not finite/contractive.

`hierarchy_owned_conditional_picard_value_enclosure(...)` additionally rejects hierarchy/order cross-wiring and points outside the bound's radial/parameter domain. Its center is exactly the hierarchy-owned four-term prefix. Its scalar radius is exactly the existing Eq. (5.8) omitted-tail norm bound.

## Agent 7 handoff

This increment does **not** derive `C_n`, a complex-strip radius, a uniform coefficient majorant, a recursive cutoff, or all-order residual convergence. The majorant inputs remain conditional theorem hypotheses. The all-order analytic-closure / Agent 7 lane should eventually replace these external values with theorem-owned bounds tied to the same assembled coefficient hierarchy and order. No sampled or fitted surrogate is acceptable for that upgrade.

## Truth boundary

The returned object is a **conditional norm-ball enclosure**, not an exact positive-order coefficient. It does not by itself materialize repaired `phi_n/U_n/Pi_n/V_n`, does not make the strict-lower hierarchy total at the next order, and does not establish Proposition 5.3, recursive-cutoff convergence, all-jets-flat, super-algebraic decay, full reconstruction, or paper-exact velocity.

Green CI only checks the implementation contract. It cannot certify that caller-provided Eq. (5.8) constants are the paper's actual analytic bounds.
