# Hierarchy-owned Eq. (5.7) `k=2` Picard value provenance

## Scope

This increment is a Stage-2 Section-5 `formal-structure` step based on `main`
commit `781fc5f22074fb864a6c0e36bc4355a0c801ecd8`.  It advances the already
landed strong-history Picard chain by one genuine nontrivial iteration.  It does
not materialize a paper-exact coefficient family or velocity.

Section 5 writes the positive-order system in Eq. (5.7) as

`d_xi W_n + xi^-1 diag(0,0,2,0,3,1) W_n = A0 W_n + A1 d_eta W_n + f_n`,

and Lemma 5.1 solves it by the Picard series generated by
`K = G(A0 + A1 d_eta)`.  The repository already owns, through the strong
Lemma-5.2 repaired lower-history hierarchy,

- hierarchy-derived `A0` and `A1`;
- hierarchy-derived `f_n` (including its first two eta derivatives);
- `W_n^(0)=G f_n` and its first two eta derivatives; and
- `W_n^(1)` together with analytic `partial_eta W_n^(1)`.

The new production bridge therefore evaluates exactly the next displayed
Picard value

`W_n^(2) = G(A0 W_n^(1) + A1 partial_eta W_n^(1) + f_n)`.

The public API accepts only `Section5LowerHistoryPhiFourthMixedHierarchy`.
It does not accept caller-supplied matrices, forcing, previous iterates,
iterate derivatives, generic cutoffs, sampled coefficient tables, fitted
families, or production finite differences.  The axis path explicitly
preflights the strong dependencies before the exact `G(0)=0` shortcut can
return, so missing Issue-#1 leading fourth/fifth-mixed data remain fail-closed.

## Verification

`tests/test_background_repaired_history_k2_picard.py` constructs the existing
Lemma 5.2 compact five-bump repair for the order-1 lower history and then solves
order 2.  It checks the new production value against a direct second
application of the already-landed Eq. (5.7) Picard primitive using only the
same hierarchy-owned `A0`, `A1`, `f_n`, `W_n^(1)`, and
`partial_eta W_n^(1)`.  The regression covers the axis and a point inside
active compact repair support and verifies that the positive-axis result is a
nontrivial new iterate rather than an alias of `W_n^(1)`.

## Truth boundary

This remains `formal-structure`.  In particular:

- genuine order-zero fourth/fifth-mixed profile data remain upstream in Issue #1;
- two explicit nontrivial Picard applications do **not** prove Picard convergence;
- `W_n^(2)` is not yet the finalized positive-order coefficient `W_n`;
- no paper recursive cutoff-scale schedule is claimed;
- no Proposition 5.3 all-jets truncation/residual-decay claim is made; and
- `full_reconstruction=false` and `paper_exact_velocity_available=false` remain mandatory.
